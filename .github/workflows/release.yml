name: Release

on:
  push:
    tags:
      - "*.*.*"
  workflow_dispatch:
    inputs:
      dry_run:
        description: Run npm publish with --dry-run
        required: true
        default: false
        type: boolean

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish @prosefly/astro-theme-lotus
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.24

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Verify tag version
        if: github.event_name == 'push'
        run: |
          PACKAGE_VERSION=$(node -p "require('./packages/astro-theme-lotus/package.json').version")
          TAG_VERSION="$GITHUB_REF_NAME"
          test "$PACKAGE_VERSION" = "$TAG_VERSION"

      - name: Check package
        run: pnpm check

      - name: Test package
        run: pnpm test

      - name: Build package
        run: pnpm build

      - name: Dry run publish to npm
        if: github.event_name == 'workflow_dispatch' && inputs.dry_run
        working-directory: packages/astro-theme-lotus
        run: npx npm@latest publish --access public --dry-run

      - name: Publish package to npm
        if: github.event_name != 'workflow_dispatch' || !inputs.dry_run
        working-directory: packages/astro-theme-lotus
        run: npx npm@latest publish --access public

      - name: Create GitHub release
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --generate-notes \
            --title "$GITHUB_REF_NAME" \
            --verify-tag
