---
title: Badge
description: Apply compact status labels that match the Lotus documentation palette.
sidebar:
  order: 4
---

import { Badge } from '@prosefly/astro-components';

`Badge` is a compact inline label for status, maturity, and contextual metadata.
It is intentionally small so it can live inside paragraphs, headings, and card
content without changing the surrounding layout.

## Import

```mdx
import { Badge } from '@prosefly/astro-components';
```

## Example

<div class="not-prose flex flex-wrap items-center gap-2">
  <span>Components can ship in stages:</span>
  <Badge color="success">Released</Badge>
  <Badge color="accent">New</Badge>
  <Badge color="warning">Needs review</Badge>
</div>

## Variants

<div class="not-prose flex flex-wrap items-center gap-2">
  <Badge variant="solid" color="accent">Solid</Badge>
  <Badge variant="soft" color="accent">Soft</Badge>
  <Badge variant="subtle" color="accent">Subtle</Badge>
  <Badge variant="outline" color="accent">Outline</Badge>
  <Badge variant="ghost" color="accent">Ghost</Badge>
</div>

## Colors

<div class="not-prose flex flex-wrap items-center gap-2">
  <Badge color="neutral">Neutral</Badge>
  <Badge color="accent">Accent</Badge>
  <Badge color="success">Success</Badge>
  <Badge color="warning">Warning</Badge>
  <Badge color="danger">Danger</Badge>
</div>

## Props

| Prop | Type | Default | Notes |
| --- | --- | --- | --- |
| `variant` | `'solid' \| 'soft' \| 'subtle' \| 'outline' \| 'ghost'` | `'soft'` | Selects the visual treatment. |
| `color` | `'neutral' \| 'accent' \| 'success' \| 'warning' \| 'danger'` | `'neutral'` | Selects the semantic color. |

## Notes

`Badge` is inline-only in the first release. Use it for status and metadata, not
as a replacement for larger navigation or filter controls.
