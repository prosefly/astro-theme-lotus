---
import ThemeModeController from './ThemeModeController.astro';

const modes = [
  { value: 'system', label: 'System' },
  { value: 'light', label: 'Light' },
  { value: 'dark', label: 'Dark' },
];
---

<div
  class="lotus-border-muted flex h-9 items-center rounded-(--lotus-radius-md) border p-0.5"
  data-lotus-theme-switch
>
  {modes.map((mode) => (
    <button
      aria-label={`${mode.label} theme`}
      class="lotus-focus-ring inline-flex h-7 w-7 items-center justify-center rounded-[calc(var(--lotus-radius-md)-2px)] text-(--lotus-text-muted) transition-colors hover:text-(--lotus-text-strong)"
      data-theme-mode={mode.value}
      title={`${mode.label} theme`}
      type="button"
    >
      {mode.value === 'light' ? (
        <svg aria-hidden="true" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8">
          <circle cx="12" cy="12" r="4" />
          <path d="M12 2v2" />
          <path d="M12 20v2" />
          <path d="m4.9 4.9 1.4 1.4" />
          <path d="m17.7 17.7 1.4 1.4" />
          <path d="M2 12h2" />
          <path d="M20 12h2" />
          <path d="m4.9 19.1 1.4-1.4" />
          <path d="m17.7 6.3 1.4-1.4" />
        </svg>
      ) : mode.value === 'dark' ? (
        <svg aria-hidden="true" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8">
          <path d="M20.5 14.5A8.5 8.5 0 0 1 9.5 3.5 8.8 8.8 0 1 0 20.5 14.5Z" />
        </svg>
      ) : (
        <svg aria-hidden="true" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8">
          <rect width="16" height="11" x="4" y="5" rx="2" />
          <path d="M8 21h8" />
          <path d="M12 16v5" />
        </svg>
      )}
    </button>
  ))}
</div>

<ThemeModeController />
