---
import rawThemeConfig from 'virtual:prosefly/lotus/config';
import type { LotusThemeConfig } from '../../lib/theme';

const themeConfig = rawThemeConfig as LotusThemeConfig;
const t = Astro.locals.t;
const currentLocale = t.locale();
const searchConfig = themeConfig.search !== false && themeConfig.search.provider === 'docsearch'
  ? themeConfig.search
  : undefined;
const indexName = searchConfig
  ? typeof searchConfig.indexName === 'string'
    ? searchConfig.indexName
    : searchConfig.indexName[currentLocale.key] ?? searchConfig.indexName[currentLocale.lang]
  : undefined;
const searchParameters = searchConfig?.searchParameters
  ? JSON.stringify(searchConfig.searchParameters)
  : undefined;
---

{searchConfig && (
  <>
    <div
      data-docsearch
      data-docsearch-api-key={searchConfig.apiKey}
      data-docsearch-app-id={searchConfig.appId}
      data-docsearch-ask-ai={searchConfig.askAi}
      data-docsearch-css-url={searchConfig.cssUrl}
      data-docsearch-disable-user-personalization={searchConfig.disableUserPersonalization}
      data-docsearch-index-name={indexName}
      data-docsearch-insights={searchConfig.insights}
      data-docsearch-js-url={searchConfig.jsUrl}
      data-docsearch-max-results-per-group={searchConfig.maxResultsPerGroup}
      data-docsearch-placeholder={t('search.placeholder')}
      data-docsearch-search-parameters={searchParameters}
      hidden
    />
    <script>
      import '../../client/search/docsearch';
    </script>
  </>
)}
